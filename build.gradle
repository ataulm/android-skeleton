buildscript {
    apply from: 'dependencies.gradle'
    repositories {
        jcenter()
    }
    dependencies {
        classpath libraries.build.kotlinGradle
    }
}

allprojects {
    repositories {
        jcenter()
    }
}
